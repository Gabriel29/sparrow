language: cpp
sudo: required

compiler:
  - clang

os:
  - linux
  # - osx

before_install:
  - sh travis/install-deps-${TRAVIS_OS_NAME}.sh

script:
  - sh travis/build-${TRAVIS_OS_NAME}.sh
  - cd ${TRAVIS_BUILD_DIR}
  - sh travis/run-tests.sh
